# {{ project_name }} — generated by Neviri-Ansi

Infrastructure: **{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · environment: **{{ env }}**

A tamper-evident audit trail: **CloudTrail** (multi-region, with log-file
validation) delivers to a **KMS-encrypted, versioned, private S3 bucket** and a
**CloudWatch** log group.

| Setting | Value |
|---------|-------|
| Region | `{{ aws_region }}` |
| Log bucket | `{{ log_bucket }}` |
| Log retention | `{{ retention_days | default(90) }}` days |

## Run
```bash
python -m venv .venv && source .venv/bin/activate
pip install ansible boto3 botocore
ansible-galaxy collection install -r collections/requirements.yml
aws configure                                   # credentials for {{ aws_region }}

ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```

> The bucket needs a policy allowing CloudTrail to write to it (the module sets a
> default); review it for your account. Log-file validation lets you prove logs
> were not altered.
