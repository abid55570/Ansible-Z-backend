# {{ project_name }} — generated by Neviri-Ansi

**{{ _meta.name }}** (v{{ _meta.version }}) · region `{{ aws_region }}`.

VPC across two AZs, a golden-image launch template, blue + green target groups, and an ALB that forwards
to blue. Deploy = bake a new AMI, launch a green ASG, then flip the ALB listener from blue to green.
Set `base_ami` (from Packer) in `group_vars/all.yml`.

## Run
```bash
ansible-galaxy collection install -r collections/requirements.yml
ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```
> Starter project — review and harden before production.
