---
# Generated by Neviri-Ansi for environment: {{ env }}
project_name: "{{ project_name }}"
aws_region: "{{ aws_region }}"
runtime: "{{ runtime | default('python3.12') }}"
table_name: "{{ table_name | default(project_name + '-table') }}"
lambda_role_arn: "{{ lambda_role_arn | default('arn:aws:iam::000000000000:role/lambda-exec') }}"
common_tags: { Project: "{{ project_name }}", ManagedBy: ansible, GeneratedBy: neviri-ansi }
