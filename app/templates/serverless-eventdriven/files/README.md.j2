# {{ project_name }} — generated by Neviri-Ansi

**{{ _meta.name }}** (v{{ _meta.version }}) · region `{{ aws_region }}` · runtime `{{ runtime | default('python3.12') }}`.

Provisions a DynamoDB table, a Lambda function, and an EventBridge schedule that invokes it.
Add an API Gateway + WAF for HTTP triggers. Provide `lambda_role_arn` and a `build/function.zip`.

## Run
```bash
ansible-galaxy collection install -r collections/requirements.yml
ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```
> Starter project — review and harden before production.
