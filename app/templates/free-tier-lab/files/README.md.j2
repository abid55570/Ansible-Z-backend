# {{ project_name }} — generated by Neviri-Ansi

Infrastructure: **{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · environment: **{{ env }}**

A minimal, Docker-ready VM to run an application on. Provision it here, then use
the **Day-2** layer (`apps.yml` / `deploy.yml`) to deploy and update your apps.

| Setting | Value |
|---------|-------|
| Region | `{{ aws_region }}` |
| VPC CIDR | `{{ vpc_cidr }}` |
| Instance type | `{{ instance_type }}` |
| SSH allowed from | `{{ office_ip | default('set office_ip') }}` |

## Layout
```
ansible.cfg
collections/requirements.yml
inventory/aws_ec2.yml
group_vars/all.yml          # your values (this environment)
playbooks: site.yml, preflight.yml
Day-2: apps.yml, deploy.yml, rollback.yml   # see DAY2.md
```

## Run
```bash
python -m venv .venv && source .venv/bin/activate
pip install ansible boto3 botocore
ansible-galaxy collection install -r collections/requirements.yml
aws configure                                   # credentials for {{ aws_region }}

ansible-playbook preflight.yml                  # checks creds + variables
ansible-playbook site.yml --check               # dry run
ansible-playbook site.yml                        # provision the VM
```

Then deploy your app onto it:
```bash
ansible-playbook deploy.yml                      # edit apps.yml first — see DAY2.md
```

> Set `ami_id` in `group_vars/all.yml` to a valid AMI for `{{ aws_region }}` before running.
> The VM installs Docker via cloud-init, so the Day-2 layer works out of the box.
