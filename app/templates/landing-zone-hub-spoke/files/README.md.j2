# {{ org_name }} — generated by Neviri-Ansi

**{{ _meta.name }}** (v{{ _meta.version }}) · region `{{ aws_region }}` · {{ spoke_count | default(3) }} spokes.

A Transit Gateway hub, a shared-services hub VPC, spoke VPCs, and a central logging bucket. Extend with
TGW attachments per spoke, centralized egress + Network Firewall, and a dedicated logging account.

## Run
```bash
ansible-galaxy collection install -r collections/requirements.yml
ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```
> Starter project — review and harden before production.
