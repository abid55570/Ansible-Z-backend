# {{ project_name }} — generated by Neviri-Ansi

Infrastructure: **{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · environment: **{{ env }}**

Centralized **AWS Backup**: an encrypted vault, a daily plan with retention, and a
**tag-based selection** so any resource tagged `Backup=true` is protected.

| Setting | Value |
|---------|-------|
| Region | `{{ aws_region }}` |
| Schedule | `{{ schedule | default('cron(0 5 * * ? *)') }}` |
| Retention | `{{ retention_days | default(35) }}` days |

## Run
```bash
python -m venv .venv && source .venv/bin/activate
pip install ansible boto3 botocore
ansible-galaxy collection install -r collections/requirements.yml
aws configure                                   # credentials for {{ aws_region }}

ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```

## Protect a resource
Tag any EC2 / RDS / EFS / DynamoDB resource with `Backup = true` and it is picked
up by the next backup run — no per-resource config needed.
