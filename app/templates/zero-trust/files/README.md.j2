# {{ project_name }} — generated by Ansible-Z

**{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · region `{{ aws_region }}`.

No bastion, no open ports: a private subnet reached via SSM interface endpoints, with a least-privilege
instance role. Add PrivateLink between services, an IdP + MFA, and mTLS to complete the zero-trust posture.

## Run
```bash
ansible-galaxy collection install -r collections/requirements.yml
ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```
> Starter project — review and harden before production.
