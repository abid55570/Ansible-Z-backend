# {{ project_name }} — generated by Neviri-Ansi

Infrastructure: **{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · environment: **{{ env }}**

A secure network foundation to build on: public + private subnets, an internet
gateway, NAT egress for the private subnet, and a locked-down base security group.

| Setting | Value |
|---------|-------|
| Region | `{{ aws_region }}` |
| VPC CIDR | `{{ vpc_cidr }}` |
| Public subnet | `{{ vpc_cidr.split('.')[0] }}.{{ vpc_cidr.split('.')[1] }}.1.0/24` |
| Private subnet | `{{ vpc_cidr.split('.')[0] }}.{{ vpc_cidr.split('.')[1] }}.10.0/24` |
| SSH allowed from | `{{ office_ip | default('set office_ip') }}` |

## Layout
```
ansible.cfg
collections/requirements.yml
inventory/aws_ec2.yml
group_vars/all.yml          # your values (this environment)
playbooks: site.yml, preflight.yml
```

## Run
```bash
python -m venv .venv && source .venv/bin/activate
pip install ansible boto3 botocore
ansible-galaxy collection install -r collections/requirements.yml
aws configure                                   # credentials for {{ aws_region }}

ansible-playbook preflight.yml                  # checks creds + variables
ansible-playbook site.yml --check               # dry run
ansible-playbook site.yml                        # provision the network
```

> This is a foundation — add instances, load balancers or databases into the
> public/private subnets. Launch app servers into the private subnet and tag
> them `Role=app` to use the Day-2 `deploy.yml` layer.
