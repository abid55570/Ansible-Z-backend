# {{ project_name }} — generated by Neviri-Ansi

Infrastructure: **{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · environment: **{{ env }}**

Host a static website on S3 — no servers to manage.

| Setting | Value |
|---------|-------|
| Region | `{{ aws_region }}` |
| Bucket | `{{ bucket_name }}` |
| Index document | `{{ index_document | default('index.html') }}` |
| Error document | `{{ error_document | default('error.html') }}` |

## Layout
```
ansible.cfg
collections/requirements.yml
inventory/aws_ec2.yml
group_vars/all.yml          # your values (this environment)
playbooks: site.yml, preflight.yml
```

## Run
```bash
python -m venv .venv && source .venv/bin/activate
pip install ansible boto3 botocore
ansible-galaxy collection install -r collections/requirements.yml
aws configure                                   # credentials for {{ aws_region }}

ansible-playbook preflight.yml                  # checks creds + variables
ansible-playbook site.yml --check               # dry run
ansible-playbook site.yml                        # create the bucket + website
```

## Publish content
```bash
aws s3 sync ./public/ s3://{{ bucket_name }}/    # upload your built site
```
The site is served at the S3 website endpoint printed by `site.yml`.

> The bucket is **public-read** so the site is reachable. Put only public,
> non-sensitive content here. Add CloudFront + a custom domain for HTTPS.
