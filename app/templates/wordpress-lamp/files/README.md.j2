# {{ project_name }} — generated by Neviri-Ansi

Infrastructure: **{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · environment: **{{ env }}**

A classic WordPress stack: an EC2 web server (Apache + PHP, WordPress fetched via
cloud-init) with a private, managed **RDS MySQL** database.

| Setting | Value |
|---------|-------|
| Region | `{{ aws_region }}` |
| VPC CIDR | `{{ vpc_cidr }}` |
| Web instance | `{{ instance_type }}` |
| DB instance | `{{ db_instance_class | default('db.t3.small') }}` |
| DB storage | `50 GB` |
| DB user | `{{ db_username }}` |
| SSH allowed from | `{{ office_ip | default('set office_ip') }}` |

## Run
```bash
python -m venv .venv && source .venv/bin/activate
pip install ansible boto3 botocore
ansible-galaxy collection install -r collections/requirements.yml
aws configure                                   # credentials for {{ aws_region }}

ansible-playbook preflight.yml                  # checks creds + variables
ansible-playbook site.yml --check               # dry run
ansible-playbook site.yml                        # provision
```

After it boots, browse to the web server's public IP and complete the WordPress
installer, pointing it at the RDS endpoint (shown in the RDS console).

> Keep `db_password` in **ansible-vault** (`ansible-vault encrypt group_vars/all.yml`),
> never in plain text. Set `ami_id` to a recent Ubuntu 22.04 image for `{{ aws_region }}`.
