# {{ project_name }} — generated by Ansible-Z

Infrastructure: **{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · environment: **{{ env }}**

A fully serverless REST API: **API Gateway → Lambda → DynamoDB**. No servers,
on-demand billing, scales to zero.

| Setting | Value |
|---------|-------|
| Region | `{{ aws_region }}` |
| Runtime | `{{ lambda_runtime | default('python3.12') }}` |
| Handler | `{{ lambda_handler | default('app.handler') }}` |
| Table | `{{ project_name }}-table` (hash key `id`) |

## Run
```bash
python -m venv .venv && source .venv/bin/activate
pip install ansible boto3 botocore
ansible-galaxy collection install -r collections/requirements.yml
aws configure                                   # credentials for {{ aws_region }}

zip api.zip app.py                               # package your handler first
ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```

> Replace `api_swagger` in `group_vars/all.yml` with your real OpenAPI spec
> (with the Lambda proxy integrations). The Lambda gets `TABLE_NAME` as an env var.
