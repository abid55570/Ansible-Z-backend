# {{ project_name }} — generated by Ansible-Z

Infrastructure: **{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · environment: **{{ env }}**

Serverless containers: an **ECS Fargate** service running `{{ container_image }}`
behind an Application Load Balancer. No EC2 hosts to patch.

| Setting | Value |
|---------|-------|
| Region | `{{ aws_region }}` |
| VPC CIDR | `{{ vpc_cidr }}` |
| Container | `{{ container_image }}` on port `{{ container_port }}` |
| Tasks | `{{ desired_count }}` |

## Run
```bash
python -m venv .venv && source .venv/bin/activate
pip install ansible boto3 botocore
ansible-galaxy collection install -r collections/requirements.yml
aws configure                                   # credentials for {{ aws_region }}

ansible-playbook preflight.yml                  # checks creds + variables
ansible-playbook site.yml --check               # dry run
ansible-playbook site.yml                        # provision
```

The ALB's DNS name (shown in the EC2 → Load Balancers console) is your service URL.

> To ship a new version, push a new image tag and update `container_image`, then
> re-run `site.yml` (the task definition + service roll forward).
