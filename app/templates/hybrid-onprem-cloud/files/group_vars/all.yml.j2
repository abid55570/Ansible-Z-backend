{% set vpc = vpc_cidr | default('10.0.0.0/16') -%}
---
# Generated by Ansible-Z for environment: {{ env }}
project_name: "{{ project_name }}"
aws_region: "{{ aws_region }}"
onprem_cidr: "{{ onprem_cidr | default('192.168.0.0/16') }}"
vpc_cidr: "{{ vpc }}"
subnet_cidr: "{{ vpc.split('.')[0] }}.{{ vpc.split('.')[1] }}.1.0/24"
common_tags: { Project: "{{ project_name }}", ManagedBy: ansible, GeneratedBy: ansible-z }
