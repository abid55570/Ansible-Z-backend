# {{ project_name }} — generated by Neviri-Ansi

**{{ _meta.name }}** (v{{ _meta.version }}) · region `{{ aws_region }}` · on-prem `{{ onprem_cidr | default('192.168.0.0/16') }}`.

Provisions a cloud VPC, a private subnet, a VPN gateway, and a route to your on-prem range.
Add a customer gateway + site-to-site VPN connection (or Direct Connect) and flow logs to finish the boundary.

## Run
```bash
ansible-galaxy collection install -r collections/requirements.yml
ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```
> Starter project — review and harden before production.
