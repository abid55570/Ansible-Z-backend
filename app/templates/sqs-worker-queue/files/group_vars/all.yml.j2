---
# Generated by Ansible-Z for environment: {{ env }}
project_name: "{{ project_name }}"
environment_name: "{{ env }}"
aws_region: "{{ aws_region }}"
lambda_runtime: "{{ lambda_runtime | default('python3.12') }}"
lambda_handler: "{{ lambda_handler | default('handler.main') }}"
lambda_zip: "{{ lambda_zip | default('worker.zip') }}"

common_tags:
  Project: "{{ project_name }}"
  Environment: "{{ env }}"
  ManagedBy: ansible
  GeneratedBy: ansible-z
