---
# Generated by Neviri-Ansi for environment: {{ env }}
project_name: "{{ project_name }}"
aws_region: "{{ aws_region }}"
cde_cidr: "{{ cde_cidr | default('10.50.0.0/24') }}"
private_subnet_cidr: "{{ cde_cidr.split('.')[0] }}.{{ cde_cidr.split('.')[1] }}.{{ cde_cidr.split('.')[2] }}.0/26"
office_ip: "{{ office_ip | default('REPLACE_WITH_YOUR_IP/32') }}"
common_tags: { Project: "{{ project_name }}", Environment: cde, ManagedBy: ansible, GeneratedBy: neviri-ansi, Compliance: pci-dss }
