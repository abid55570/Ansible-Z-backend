# {{ project_name }} — generated by Neviri-Ansi

**{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · region `{{ aws_region }}`.

An isolated cardholder-data environment: VPC with no IGW, a private subnet, a KMS key, a restricted
security group (SSH from `{{ office_ip | default('your IP') }}` only), an encrypted log bucket, and CloudTrail.
Add FIM, anti-malware, and quarterly ASV scans to complete PCI-DSS coverage.

## Run
```bash
ansible-galaxy collection install -r collections/requirements.yml
ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```
> Starter project — this is scaffolding, not a PCI certification. Layer the remaining controls + evidence.
