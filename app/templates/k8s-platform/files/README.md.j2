# {{ cluster_name }} — generated by Ansible-Z

**{{ _meta.name }}** (v{{ _meta.version }}) · region `{{ aws_region }}`.

Provisions a VPC across two AZs, an EKS cluster, and a managed node group ({{ node_count | default(3) }} nodes).
Set `cluster_role_arn` and `node_role_arn` in `group_vars/all.yml`. Add ingress + WAF and NetworkPolicies to harden.

## Run
```bash
ansible-galaxy collection install -r collections/requirements.yml
ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```
> Starter project — review and harden before production.
