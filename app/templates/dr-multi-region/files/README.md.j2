# {{ project_name }} — generated by Neviri-Ansi

**{{ _meta.name }}** (v{{ _meta.version }}) — primary region `{{ aws_region }}` + DR region `{{ dr_region }}`.

Provisions a VPC and a versioned backups bucket in each region. Extend with cross-region
replication, RDS read-replicas, and a Route 53 failover record for a complete DR posture.

## Run
```bash
ansible-galaxy collection install -r collections/requirements.yml
ansible-playbook preflight.yml
ansible-playbook site.yml --check
ansible-playbook site.yml
```
> Starter project — review and harden before production.
