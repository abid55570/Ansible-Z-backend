# {{ project_name }} — generated by Ansible-Z

Infrastructure: **{{ _meta.name }}** (v{{ _meta.version }}, PCI: {{ _meta.pci }}) · environment: **{{ env }}**

Auto-scaling app tier across two Availability Zones behind an ALB, with an RDS Multi-AZ database.

| Setting | Value |
|---------|-------|
| Region | `{{ aws_region }}` |
| VPC CIDR | `{{ vpc_cidr }}` |
| ASG size | `{{ min_size | default(2) }}`–`{{ max_size | default(4) }}` |

## Run
```bash
python -m venv .venv && source .venv/bin/activate
pip install ansible boto3 botocore
ansible-galaxy collection install -r collections/requirements.yml
aws configure                                   # credentials for {{ aws_region }}

ansible-playbook preflight.yml
ansible-playbook site.yml --check               # dry run
ansible-playbook site.yml
```

> Starter project — put `db_password` in `ansible-vault`, attach a launch-template user-data / AMI for your app,
> and review sizing before production.
